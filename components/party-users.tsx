export default function PartyUsers() {
    // grab user's friends from the data base
    // on click, they get added to the party and removed from this list.
        // I think use onAdd function from the parent
    // what does the parent send to this? the party? no?
        // yes cuz we need to know which players have been added to the party to start the party and get their games?
    return (
        <div>
            THE PARTY USERS ARE HERE
        </div>
    )
}